/*
export const clearHighlight = () => ({
  type: "attach/clear-highlight",
});

export const selectOption = (index) => ({
  type: "attach/select-option",
  payload: { index }
});

export const clearOptions = (index) => ({
  type: "attach/clear-options",
  payload: { index }
});
*/